class STT(object):

    def write(self):
        """
        This function takes a message as an argument and converts it to
        text depending on the OS.
        """
        raise NotImplementedError()
